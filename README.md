# Exit makefile



## How to use it
Save the file as Makefile (no extension), then run from the same directory:

### Run everything at once
`make all`

### Or run individual targets

`make safari`

`make ssh`

`make chrome`# ExitMac
